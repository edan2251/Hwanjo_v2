# Hwanjo v2 — Step 0: 환경 확인과 저장소 작업 기준 준비

이 파일은 Codex CLI에 전달하는 작업 요청이다. 지금은 게임 구현 단계가 아니다.
현재 프로젝트를 조사하고, 짧은 프로젝트 지침·Skill·기준 문서를 준비한 뒤 멈춘다.
기본공격, 반응 시스템, 더미, 적, 아트 제작은 이 단계에서 시작하지 않는다.

## 1. 작업 대상과 경계

- 사용자 지정 Git 루트: `C:\Users\JEYJEY\Desktop\Hwanjo_v2`
- Unity 프로젝트: Git 루트 아래 `Hwanjo_v2_Test`
- 원격 저장소: `https://github.com/edan2251/Hwanjo_v2`
- 이번 작업 브랜치: `prototype/element-lab-v01`
- 확인된 원격 ProjectVersion.txt: `6000.3.23f1 (09d2ecc7fb28)`
- 원격 manifest에는 URP, Input System, 2D Animation, Test Framework가 있다.
  로컬의 실제 파일 및 Import 성공 여부는 따로 확인한다.
- 기존 환조 v1 저장소와 전투 프로토타입은 별도 자료다. 찾거나 복사하거나 수정하지 않는다.
- Git 상태와 기존 AGENTS.md/Skill 유무를 먼저 읽는다. 기존 사용자 지침과 충돌하면 보고한다.
- Git add/commit/push/merge/switch/reset/restore/clean/stash는 하지 않는다.
  작업 브랜치가 다르면 파일 변경 전에 중단하고 현재 브랜치를 보고한다.
- 이 요청 파일만 untracked인 것은 정상적인 준비 상태다. 파일은 그대로 보존한다.
  다른 사용자 변경이 있으면 무엇인지 먼저 구분하고, 겹치는 파일을 덮어쓰지 않는다.

## 2. 변경을 허용하는 파일

기존 파일이 있으면 읽은 뒤 최소 변경한다. 아래 범위 밖의 코드는 쓰지 않는다.

- 루트 `.gitignore`: 이번에 사용할 `/artifacts/`, `/Builds/`가 제외되지 않을 때만 추가.
- 루트 `AGENTS.md`: 짧은 저장소 공통 지침.
- `.agents/skills/hwanjo-v2-prototype/SKILL.md`: 저장소 전용 지침형 Skill 하나.
- `docs/PROTOTYPE_SCOPE.md`: 아래 사용자 결정과 미정사항을 구분한 단일 기획 기준.
- `docs/ENVIRONMENT_AUDIT.md`: 로컬 환경 조사 및 실제 검증 결과.
- `docs/HANDOFF.md`: 이번에 한 일, 남은 일, 다음 작업 시작점.
- `README.md`: 프로젝트 경로와 위 문서들의 간단한 안내만 보완.
- `artifacts/setup/<실제 실행 식별자>/`: 진단 로그와 허용된 임시 결과.

`Assets`, `Packages`, `ProjectSettings`를 의도적으로 수정하지 않는다.
Unity Import가 자동 변경한 추적 파일이 발견되면 목록과 diff를 보고하고,
임의로 원복하거나 성공 조건에 맞추기 위해 숨기지 않는다.
추적되지 않은 캐시 생성은 가능하나 기존 캐시를 삭제하거나 전체 재임포트를 강제하지 않는다.

## 3. 환경 확인

1. Git 루트, 브랜치, HEAD, 원격 주소, staged/unstaged/untracked 상태를 확인한다.
2. Unity의 Assets/Packages/ProjectSettings와 .meta 추적 상태를 확인한다.
3. 루트와 Unity 프로젝트 내부의 `.gitignore`를 각각 확인한다.
   내부 `.gitignore`가 이미 존재하면 그대로 사용한다. 루트 규칙만 보고
   중첩 Unity 프로젝트의 Library가 무시되지 않는다고 섣불리 판단하지 않는다.
4. `git check-ignore`와 추적 파일 목록으로 캐시·빌드·로그 제외를 확인한다.
   Assets, .meta, Packages의 필요한 파일, ProjectSettings는 제외하지 않는다.
5. ProjectVersion.txt, Packages/manifest.json, packages-lock.json을 읽어 실제 버전을 기록한다.
   기본 패키지를 불필요하다고 일괄 삭제하거나 업데이트하지 않는다.
6. 렌더 파이프라인과 실제 Renderer 설정, 입력 설정, 초기 씬, 기존 게임 코드 유무를 조사한다.
   URP 패키지 존재만으로 2D Renderer 연결 성공이라고 판단하지 않는다.
7. 현재 PC에서 해당 버전의 Unity.exe 설치 경로와 Windows 빌드 준비 상태를 확인한다.
   설치 경로가 이전 PC와 같다고 가정하지 않는다. 프로그램을 설치하지 않는다.
8. 현재 실행 중인 해당 프로젝트의 Unity Editor가 있으면 사용자에게 정상 종료를 요청한다.
   다른 작업의 Unity/Hub/라이선싱 프로세스를 종료하거나 잠금 파일을 삭제하지 않는다.
9. 가능하면 기존 프로젝트를 Batch Mode로 한 번 열어 Import/컴파일과 정상 종료를 검증한다.
   전체 진단 재귀 루프, 복사본 여러 개, 테스트 게임 빌드 제작은 하지 않는다.
   stdout/stderr와 Unity 전체 로그, 종료 코드를 구분해 기록한다.
   `-ignorecompilererrors` 또는 버전/패키지 변경으로 통과시키지 않는다.
10. 승인·라이선스·샌드박스에서 막히면 최초 원인과 필요한 권한을 기록한다.
    가능한 경우 해당 Unity 진단 명령만 승인 요청한다. 허용되지 않으면 그 항목은 BLOCKED.
    같은 오류로 계속 재시도하거나 전역 권한·라이선스·Codex 설정을 변경하지 않는다.

이 단계는 사람이 지켜보는 대화형 환경 준비다. 무인 야간 실행기를 만들지 않는다.
프로젝트 파일 쓰기와 Unity 실행의 권한은 별개로 확인한다.
기존 테스트가 없다면 '테스트 0개 / 미작성'이라고 기록한다.
빈 프로젝트 컴파일 성공을 전투·반응·빌드·성능 검증 성공으로 보고하지 않는다.
현재 사용 가능한 이미지 입력·생성 도구는 명세상 노출 여부만 보고한다.
이 단계에서는 실제 이미지 생성 요청, 샘플 이미지 생성, 외부 유료 API 호출을 하지 않는다.

## 4. PROTOTYPE_SCOPE.md에 옮길 사용자 기준

### 확정된 이번 실험의 기준

- 목적: 탐험용 속성 반응과 검흔 재타격이 이해하기 쉽고 응용할 수 있는지 검증.
- 동양풍, 사방신을 유지하되 같은 인간 플레이어와 근접 검 하나를 사용한다.
- 4무기/2폼 장착/퀵스왑 게이지/출발 폼 잔향/현무 전용 패링을 가져오지 않는다.
- 이번 테스트 속성: 불 / 물 / 바람 / 얼음.
- 기본 베기: J 짧게, 단타 1회.
- 차지: J Hold, 차지 기준 0.5초.
- 차지 단독 최대 도달거리: 기본 베기 R의 1.75배.
  동일 플레이어 기준점에서 잰 전체 도달거리이며 검 끝의 추가 이동량이 아니다.
- 동시에 존재하는 검흔: 1개.
- 속성 전환: 1=불, 2=물, 3=바람, 4=얼음. 즉시 선택, 전환 연출/쿨타임 없음.
- 반응: 한 입력으로 상태 변화 최대 한 단계. 같은 입력에서 건조 후 점화까지 연쇄하지 않는다.
- 검흔: 차지 뒤 공간에 남긴 반응 대상. 다른 속성의 베기가 실제로 닿아야 반응한다.
- 대상 이름보다 특성(Trait)·현재 상태(State)·입력 효과(Effect)로 공통 반응을 판정한다.
- 가연성은 특성, 건조/젖음은 상태로 구분한다.

### 사용자가 추가로 요청한 범위

- 특성을 직접 바꾸는 더미 박스 1개와 테스트 UI.
  프리셋·재질은 드롭다운, 동시에 가질 수 있는 특성은 다중 선택/토글이 가능한 구성 후보.
  Apply/Reset 및 현재 효과·반응·상태 표시가 필요하다.
- 상태 변화를 읽을 수 있는 VFX, 기본 타격감과 프로토타입 시각 품질.
- 기본 적에게도 속성 반응 적용:
  - 불: 연소와 지속 피해.
  - 물: 이동 및 공격 속도 둔화.
  - 바람: 넉백 또는 짧은 띄우기. 구체 형태는 추천 후 시험.
  - 얼음: 동결. 세부 성립 조건은 아래 미정사항.
- 사용자 캐릭터 레퍼런스에 맞춘 약 48×48 동양풍 픽셀 캐릭터와 필요한 애니메이션.
  아직 이 작업에 캐릭터/화풍 참고 이미지가 제공되었다고 가정하지 않는다.
  저장소 폴더 스크린샷은 캐릭터 레퍼런스가 아니다.

### 구현을 위한 추천 범위 — 최종 채택·일정 확정 아님

- 작은 반응 실험실과 짧은 응용 구간. 큰 메트로베니아 월드 제작은 하지 않는다.
- 환경 후보: 덩굴·밧줄·나무상자·물웅덩이·풍차/송풍 장치·얼릴 수 있는 가동 장치.
- 검흔의 우선 비교: 불 검흔+바람, 물 검흔+얼음.
- 타격 판정 표시, 최근 반응 기록, 대상 초기화, 실험실 전체 Reset.
- 적 1종부터 시험. 보스·스토리·저장·해금 동선은 이번 제작에서 제외.

### 아직 확정하지 않은 것 — 이번 준비 단계에서 임의로 채택하지 말 것

- 검흔 지속시간: 이전 2초는 추천값이며, 사용자가 명확히 확정한 것은 최대 1개다.
- 탭/홀드 입력 발동 시점: 0.5초 기준만으로 눌렀을 때/뗐을 때 동작이 정해진 것은 아니다.
- 차지 중 속성 변경·이동·대시 취소·공중 사용.
- R의 실제 값, 검흔 생성 위치·크기·수명·소비/변환·새 검흔으로 교체하는 규칙.
- 한 단계 반응의 묶음 정의. 소화와 젖음이 한 전이인지, 각각 별도인지 구분 필요.
- Dry/Wet/Frozen/Burning의 공존과 복귀, 액체 물의 상태 구분.
- 물의 둔화는 적의 반응 결과다. Wet인 모든 오브젝트를 자동 감속시키지 않는다.
- 마른 적에게도 바로 얼음 동결을 줄지, 젖은 적만 동결하고 마른 적은 둔화시킬지 미정.
  후자는 앞선 추천안일 뿐 사용자 확정으로 바꾸지 않는다.
- 바람의 적 이동은 일반 물리 피해와 별개의 반응 조건·횟수로 정의 필요.
- 바람으로 날리는 검흔의 사거리·피해·충돌, 얼음 구조의 위치·발판 허용·무한 생성 방지.
- 캐릭터 레퍼런스, 외형, 반드시 유지할 특징, 아트의 실제 사용 권리.

## 5. AGENTS.md와 Skill의 역할

### AGENTS.md

저장소 공통 규칙과 문서 위치만 짧게 쓴다.
`Hwanjo_v2_Test`가 Unity 프로젝트이며, 루트는 Git/문서/Skill 위치라는 점을 명시한다.
새 기획에 v1 시스템을 임의 이식하지 않기, 원본 보존, 최소 수정, 검증과 문서화,
사용자 요청 없는 Git 기록/원격 변경 금지를 담는다.
로컬 절대 경로는 환경 보고서에 기록하되, 공통 규칙은 저장소 상대 경로로 쓴다.
최신 수치·전체 기획표는 AGENTS.md에 중복 복사하지 않는다.

### hwanjo-v2-prototype Skill

저장소 안 `.agents/skills/hwanjo-v2-prototype/SKILL.md`에 지침형 하나만 만든다.
YAML frontmatter에는 name과 구체적인 description을 넣는다.
적용 대상은 이 저장소의 v2 프로토타입 구현·수정·회귀 검증 작업이다.
일반 대화, 이번에 허용하지 않은 게임 구현, v1 폼 전투 제작을 자동 시작하지 않는다.
내용은 작업 전 문서/변경 확인 → 지정 범위 구현 → 실제 검증 → 결과·미정 기록 순서다.
기획과 숫자는 `docs/PROTOTYPE_SCOPE.md`를 참조하고 별도의 원본을 만들지 않는다.
성공·실패를 구분하는 기준을 쓰되 아직 실행하지 않은 명령을 검증됐다고 적지 않는다.
거대한 런처·병렬 에이전트·스크립트 묶음은 만들지 않는다.

## 6. 이전 시행착오를 반복하지 않는 기록 방식

- PowerShell에 전달할 경로는 실제 절대 경로 또는 실행 위치를 명확히 한 경로로 쓴다.
- .NET 파일 I/O에서 PowerShell 현재 위치와 프로세스 현재 위치가 같다고 가정하지 않는다.
- 기존 인코딩·줄바꿈을 가급적 유지한다. 의미 없는 BOM/빈 줄 정리에 작업을 소모하지 않는다.
- Git의 stdout 파일 목록과 stderr 경고를 분리한다. CRLF 경고를 삭제 파일로 해석하지 않는다.
- Unity 실행 실패는 권한/라이선스/컴파일/런타임으로 분류한다.
- 경고를 숨겨 성공으로 만들지 않고, 실제 실패가 아닌 경고도 실패라고 단정하지 않는다.
- credentials, 토큰, API key, 개인 설정 원문을 문서·로그·Git에 기록하지 않는다.
- UI 테스트와 이미지 생성은 단순 도구 노출 확인과 실제 동작 확인을 구분한다.
- 게임 아트·레퍼런스가 없어도 환경 준비는 진행한다. 없는 이미지를 대신 지어내지 않는다.

## 7. 이번 완료 조건과 보고

게임은 아직 만들지 않고 위 파일과 조사 결과까지만 준비한다.

최종 답변에는 다음을 한국어로 정리한다.

1. 확인한 Git 루트·브랜치·HEAD·원격 주소.
2. Unity 버전·실제 Editor 경로·2D 렌더러·Input System·Test Framework 상태.
3. Batch Mode 컴파일을 실제 실행했는지, 종료 코드·로그 위치·남은 오류.
4. 생성/수정한 문서·Skill·ignore 파일과 사용자 변경 보존 여부.
5. 이미지 입력/생성 도구의 확인 수준, 캐릭터 레퍼런스 준비 상태.
6. 구현 전에 결정할 항목을 '미정'으로 적은 목록. 이미 확정한 7개 결정을 다시 묻지 않는다.
7. 다음 구현 단계 준비 상태: READY / BLOCKED와 구체 이유.

READY는 환경과 기준 문서 준비 완료만 뜻한다. 게임 제작·아트 완성·테스트 통과를 뜻하지 않는다.
Git commit/push는 하지 말고 diff 요약과 git status를 보고한 뒤 멈춘다.
