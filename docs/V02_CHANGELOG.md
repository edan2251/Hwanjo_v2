# Element Lab v0.2 변경 기록

실행 요청: `element-lab-v02/04_CODEX_IMPLEMENT.md`와 2026-09-25 사용자 메시지. A→B→C 순차 구현. 실제 최종 검증은 V02_VALIDATION.md 참조.

- 시작 HEAD: `76deb8db4d5bec223cb98657596b2c0c4d0e56ba`, 실제 브랜치 `prototype/element-lab-v01`. 기대 v02 브랜치와 다르지만 사용자 지시에 따라 변경하지 않는다. staged/unstaged/untracked 모두 없음.
- 보존 기준: `artifacts/element-lab-v02/20260925T233641+0900/baseline.json`, 322개 파일 해시. v0.1 빌드, 원본, 패키지 문서, AGENTS/Skill 포함.
- 원본의 실제 위치는 `element-lab-v01/`이며 `references/element-lab-v01/`는 없다. 원본 5장을 모두 열었다. v0.1 실제 화면은 이전 실행의 `captures-delivery/Idle-HeroOtherPoses_00-20260925-005107-376.png`를 열어 확인했다.
- Python 명령은 PATH에 없어 최초 호출 실패(exit1). 설치하지 않고 기존 `C:/Users/JEYJEY/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`를 사용한다.

## A 조사와 변경

기존 O는 발 위치+(0,0.62), 몸 collider 0.48×0.88, R=1.6. 수평 공격은 폭1.05의 사각 판정, 차지2.8(1.75R), 검흔 중심0.875R/높이1.1. 수면은 물 영역 전체 trigger이고 한글/영문 혼용 IMGUI와 Windows 동적 글꼴을 사용했다.

이번 사용자 결정은 기본/차지1R와 네 방향. R=1.6, 공격 피해10, 이동/점프/대시/적 상태 수치는 유지한다. 입력은 J release에서 방향·속성을 고정한다. W/S 동시 중립, 수직 우선. 공중 짧은 단타는 유지하고 긴 대기는 취소한다. 가림은 방향 사각 판정에 닿은 표면별 ray 검사다. 고체 표면 자체는 맞힐 수 있다.

임시안: 검흔 중심0.6R, 방향 길이0.25R/측면0.8, 수명2초. 벽에 막히면 유효 공간으로 줄이고 공간이 없으면 기존 슬롯을 유지한다. 냉기 흔적은 발판이 아니며 열로 소멸한다. 물→얼음 구조만 지지면에서 collider를 얻고 최초 만료시각을 유지한다. v0.1의 얼음 직접 발판/열로 물 생성 동작과 의도적으로 다르다. 반응 지원표는 `TraceSupport`를 실제 수신 정책과 UI가 공유한다.

## 진행 상태

**A/B/C PASS.** 최종 EditMode41/41, PlayMode38/38(각 fail0/skip0), Windows 빌드와 실제 재생·UI 클릭·연속 화면 열람 완료. 실행·중간 실패·최종 XML과 빌드/렌더 검증은 `V02_VALIDATION.md`에 기록했다. 사람의 재미·최종 미술 승인·장시간 안정성은 별도다.

## B/C 구현 변경

탐험8방, 최초 불/물·얼음·바람 획득, 방문 지도/발견2개/체크포인트2개/성소 지름길/세션 복구를 기존 실험실과 별도 루트에 연결했다. 물길13-unit과 위쪽 육로, 위 베기 밧줄, 물 소화 문, 아래 베기 수면, 선택 수직 불 전달/임시 받침을 실제 지형으로 배치했다. 자동 입력 전체 왕복 중 E05/E06/E02의 충돌 문제를 발견해 계단·통행 여유만 수정했다. 핵심 이동 스펙과2초 수명은 유지했다.

검 sprite1개, 방향 몸체36프레임/공중 아래6프레임, 이끼 석재7종+가지1종, 원경/중경 새 레이어를 별도 V02 폴더에 추가했다. 기존 보행·점프·대시·피격·사망 자산은 재사용하며 손 앵커/검 각도/좌우 반전/방향 판정을 함께 적용했다. 카메라0.12/0.08 SmoothDamp와 F5 즉시 비교, 절대 원점 기반3층 패럴랙스, 방 반시야 clamp/스냅을 연결했다. 생성 도구6회, 상세 manifest와 실제 연속 프레임은 별도 기록한다.

원본/생성 소스/v0.1 빌드와 사용자 요청 패키지·Skill·AGENTS는 보존한다. 빌드 helper는 기존 씬을 명시해 V02 폴더로만 출력한다. 원격 작업·Git add/commit/push/브랜치 변경을 하지 않았다.
